[package]
name = "numparse"
version = "2.0.0"
edition = "2021"
publish = false

# Standalone workspace root so the crate resolves on its own
# (single-crate target) and does not attach to any ancestor workspace.
[workspace]
